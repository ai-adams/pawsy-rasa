version: "3.1"

actions:
  - action_hello_world

intents:
  - EXTERNAL_welcome
  - greet
  - goodbye
  - bot_challenge

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
